export class Employee {
    id: number;
    firstName: string;
    lastName: string;
    emailId: string;
    active: boolean;

    constructor(id, firstName, lastName, emailId, active){
        this.id = id;
        this.firstName = firstName;
        this.firstName = firstName;
        this.active = active;
    }

}